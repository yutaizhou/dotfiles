- defaults:
    link:
        create: true
        #force: true
        relink: true
- link:
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.shell:
    ~/.vimrc: 

- shell: []
- clean: ['~']
