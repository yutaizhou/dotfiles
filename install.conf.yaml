- defaults:
    link:
        create: true
        #force: true
        relink: true
- link:
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.condarc:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.inputrc: 
    ~/.shell:
    ~/.vimrc: 

- shell: []
- clean: ['~']
